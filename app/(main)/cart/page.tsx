import { CartClient } from './client'

export const metadata = {
  title: 'Shopping Cart - RestaurantBook',
  description: 'View and manage your cart items',
}

export default function CartPage() {
  return <CartClient />
}
